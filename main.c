#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
	int fact,i,ans;
	printf("enter no:\n");
	scanf("%d",fact);
	
	for(i=1;i<=fact;i++){
		ans=ans*i;
		
		
	}
	print(ans);
	return 0;
}
